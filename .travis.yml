sudo: required
language: node_js
node_js:
  - "10.8"
cache:
  directories:
    - "node_modules"
services:
  - docker
before_install:
  - sudo service postgresql stop
  - docker-compose -f prisma/docker-compose.yml up -d
  - docker ps
  - sleep 20
before_script:
  - yarn prisma deploy